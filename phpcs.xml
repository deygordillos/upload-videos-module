<?xml version="1.0"?>
<ruleset name="Video Upload API Coding Standard">
    <description>PSR-12 Coding Standard for Video Upload API</description>

    <!-- Base standard -->
    <rule ref="PSR12"/>

    <!-- Paths to check -->
    <file>app</file>
    <file>tests</file>

    <!-- Exclude vendor and other directories -->
    <exclude-pattern>vendor/*</exclude-pattern>
    <exclude-pattern>uploads/*</exclude-pattern>
    <exclude-pattern>coverage/*</exclude-pattern>

    <!-- Show progress -->
    <arg value="p"/>

    <!-- Use colors -->
    <arg name="colors"/>

    <!-- Show sniff codes in all reports -->
    <arg value="s"/>
</ruleset>
